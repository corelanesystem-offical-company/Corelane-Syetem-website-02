# Netlify redirects

# www → non-www (canonical domain enforcement)
https://www.corelanesystem.com/* https://corelanesystem.com/:splat 301!

# SPA fallback — must be LAST so explicit redirects above take priority
/*    /index.html   200
